//This Higher order component is just a replacement for the wrapping div that is usually present when returning JSX
//by the way props always have children

const auxi = (props) => props.children;

export default auxi